import { ExecutionBGPipe } from './execution-bg.pipe';

describe('ExecutionBGPipe', () => {
  it('create an instance', () => {
    const pipe = new ExecutionBGPipe();
    expect(pipe).toBeTruthy();
  });
});
